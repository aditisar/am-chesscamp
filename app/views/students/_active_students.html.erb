<h3> <small> Active </small></h3><br />
<table class="table">
  <tr>
    <th>Name</th>
    <th>USCF Rating</th>
    <th>Age</th>
    <th></th>
  </tr>

<% @active_students.each do |student| %>
  <tr> 
    <td><%= link_to student.name, student_path(student) %></td>
    <td align="center"><%= student.rating == 0 ? "Unrated" : student.rating %></td>
    <td align="center"><%= student.age %></td> 
    <td><%= link_to 'Edit', edit_student_path(student), id: 'editbutton', class: "button tiny radius" %></td>
  </tr>
<% end %>
</table>

<br />
<!-- Add the pagination using will_paginate gem -->
<span class="paginate">
<%= will_paginate @active_students, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>